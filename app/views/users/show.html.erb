<p class='fs-5 fw-bold text-white'>The details for your profile are below</p>

<div class='mb-3 mt-4 text-white bg-black mb-5 py-5 px-5 border border-warning shadow-lg p-3 mb-5 bg-black rounded'>

 
  <% if current_user.image.attached? %>
    <p class='fs-4 fw-bold'>Your profile pic:</p>
    <%= image_tag current_user.image, class: "img-fluid img-avatar rounded-circle "%>
  <% end %>
  <h1>Name is: <%=current_user.name %></h1>
  <h1>Email is:<%= current_user.email %> </h1>

  <div class='text-center mt-5 mb-1'>
    <p class='fs-4 fw-bold'>Would you like to change these: <%= button_to "SETTINGS ", edit_user_path(current_user), method: :get, class: 'btn btn-outline-info text-center' %></p>
  </div>
</div>
